import React from "react";
import {Box, Button} from "@mui/material";

export default function Login() {
    return (
        <Box sx={{display: 'flex', background: 'yellow', marginLeft: 12}}>
            <p>Login</p>
            <Button />
        </Box>
    );
}